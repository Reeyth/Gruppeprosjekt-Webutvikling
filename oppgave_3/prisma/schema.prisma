generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator dbml {
  provider = "prisma-dbml-generator"
}

model employee {
  id        Int      @id @default(autoincrement()) @unique
  name      String
  rules     String
}
